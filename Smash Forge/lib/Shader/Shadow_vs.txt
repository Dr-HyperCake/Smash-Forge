#version 330
in vec3 vPosition;

uniform mat4 lightSpaceMatrix;
uniform mat4 modelMatrix;
uniform mat4 mvpMatrix; // modelview matrix

void main()
{
    gl_Position = lightSpaceMatrix * vec4(vPosition, 1.0f); //lightSpaceMatrix * mvpMatrix * vec4(vPosition, 1.0f);
}
